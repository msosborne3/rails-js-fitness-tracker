<%= form_for(@workout) do |f| %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="field">
  <%= f.label :calories_burned %> <br>
  <%= f.text_field :calories_burned %>
  </div>

  <div class="actions">
  <%= f.submit %>
  </div>

<% end %>